# Evidence for /pt/precos/

Screenshots of the carrier price pages cited in `_data/precos_br.yml`, taken 2026-09-23.

Caveats: captured from the US (not in-country) with Chrome auto-translate on, so
the visible text is English. The two `header-only` files show the page but not
the price cards; the prices for those rows come from the page text extracted at
the same time. Replace with in-country pt-BR screenshots when someone in Brazil
can take them.
